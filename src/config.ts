export enum Platform {
    HOME = 0,
    ADMIN = 1,
}

export enum AuthTab {
    LOGIN = 0,
    REGISTER = 1,
}

export enum AccountType {
    EMAIL = 0,
    TELEPHONE = 1
}

export enum UserMenu {
    MYUSERINFO = 0,
    MYVIDEO = 1,
    MYDYNAMIC = 2,
    SUBSCRIPTION = 3,
    HISTORY = 4,
    MESSAGE = 5
}

export enum Sex {
    MAN = 0,
    WOMAN = 1
}