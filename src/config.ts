export enum Platform {
    HOME = 0,
    ADMIN = 1,
}

export enum AuthTab {
    LOGIN = 0,
    REGISTER = 1,
}

export enum AccountType {
    EMAIL = 0,
    TELEPHONE = 1
}

export enum UserMenu {
    MYUSERINFO = 0,
    MYDYNAMIC = 1,
    SUBSCRIPTION = 2,
    HISTORY = 3,
    MESSAGE = 4
}